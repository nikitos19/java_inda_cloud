server:
  port: 8082
spring:
  application:
    name: order-service
  profiles:
    active: default
  cloud:
    eureka:
      instance:
        preferIpAddress: true
      client:
        registerWithEureka: true
        fetchRegistry: true
        serviceUrl:
          defaultZone: http://localhost:8761/eureka/